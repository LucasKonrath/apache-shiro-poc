[main]
# Minimal config. IniSecurityManagerFactory will create a SecurityManager with IniRealm.

[users]
# username = password, roles
alice = password123, admin
bob = secret, user

[roles]
# role = permission definitions
admin = *
user = document:read
